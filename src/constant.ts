enum MenuInfo {
  COLLAPSE = 'collapse',
  MENU_RECORDS = 'menu_records',
  MAX_RECORDS = 10
}

enum UserInfo {
  USERNAME = 'v-username',
  PASSWORD = 'v-password',
}

enum API {
  USER_LOGIN = '/login',
}
export { MenuInfo, UserInfo, API }